<?xml version="1.0" encoding="utf-8"?>

<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:ViewModels="clr-namespace:CounterJO.ViewModels"
             x:Class="CounterJO.Views.AllCountersPage"
             x:DataType="ViewModels:CountersViewModel"
             NavigatedTo="ContentPage_NavigatedTo"
             Title="Your Counters">
    
    <ContentPage.BindingContext>
        <ViewModels:CountersViewModel>
            
        </ViewModels:CountersViewModel>
    </ContentPage.BindingContext>
    
    <ContentPage.ToolbarItems>
        <ToolbarItem
            Text="Add"
            Command="{Binding NewCommand}"
            IconImageSource="{FontImage Glyph='+', Size=22, Color=Black}">
        
        </ToolbarItem>
    </ContentPage.ToolbarItems>
    
    <ContentPage.Content>
        <CollectionView x:Name="countersCollection"
                        ItemsSource="{Binding AllViewCounters}"
                        Margin="20"
                        SelectionMode="Single"
                        SelectionChangedCommand="{Binding SelectCounterCommand}"
                        SelectionChangedCommandParameter="{Binding x:DataType='CollectionView', Source={RelativeSource Self}, Path=SelectedItem}">

            <CollectionView.ItemsLayout>
                <LinearItemsLayout Orientation="Vertical" ItemSpacing="10"></LinearItemsLayout>
            </CollectionView.ItemsLayout>
            <CollectionView.ItemTemplate>
                <DataTemplate x:DataType="ViewModels:CounterViewModel">
                    <StackLayout>
                        <StackLayout Orientation="Horizontal" Spacing="10" VerticalOptions="Center">
                            <Frame WidthRequest="20" HeightRequest="20" CornerRadius="10" BackgroundColor="{Binding CustomColor}" BorderColor="Black" HasShadow="False" VerticalOptions="Center"/>
                            <Label Text="{Binding Name}" FontSize="16" VerticalOptions="Center"/>
                        </StackLayout>
                        <Label Text="{Binding Count}"
                               FontSize="22"></Label>
                        <Grid ColumnDefinitions="*,*,*,*" ColumnSpacing="1">
                            <Button Grid.Column="0"
                                    Text="Add"
                                    Command="{Binding AddRelayCommand}">
                            </Button>
                            <Button Grid.Column="1"
                                    Text="Substract"
                                    Command="{Binding SubstractRelayCommand}">

                            </Button>
                            <Button Grid.Column="2"
                                    Text="Reset"
                                    Command="{Binding ResetRelayCommand}">

                            </Button>
                            <Button Grid.Column="4"
                                    Text="Delete"
                                    Command="{Binding DeleteRelayCommand}">

                            </Button>
                        </Grid>
                    </StackLayout>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </ContentPage.Content>
</ContentPage>
